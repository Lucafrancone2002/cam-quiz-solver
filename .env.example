# Vision & AI Orchestrator Agent (/api/analyze) — Google Gemini (gratuito entro
# i limiti della free tier). Genera una chiave su https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Modello usato per l'estrazione visione (opzionale, ha un default nel codice).
GEMINI_VISION_MODEL=gemini-1.5-flash
